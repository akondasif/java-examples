server:
  port: ${PORT:8888}

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/adriannovegil/spring-cloud-example-config-repository.git
